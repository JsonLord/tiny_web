```python
from gradio_client import Client
client = Client("diamond-in/Browser-Use-mcp")
```

---

# 1. **/get_cookies**
_Retrieve all cookies from the specified domain as a JSON string._

### Parameters
- `url` (str)
- `use_persistent` (bool)

### Code Snippet
```python
result = client.predict(
    url="https://example.com",
    use_persistent=False,
    api_name="/get_cookies"
)
print(result)
```

---

# 2. **/set_cookies**
_Inject cookies into the browser for a specific domain using a JSON formatted string._

### Parameters
- `url` (str)
- `cookies_json` (str)
- `use_persistent` (bool)

### Code Snippet
```python
result = client.predict(
    url="https://example.com",
    cookies_json='[{"name": "test", "value": "123"}]',
    use_persistent=False,
    api_name="/set_cookies"
)
print(result)
```

---

# 3. **/execute_js**
_Run custom JavaScript code on the target page and return the execution result._

### Parameters
- `url` (str)
- `script` (str)
- `use_persistent` (bool)

### Code Snippet
```python
result = client.predict(
    url="https://example.com",
    script="return {title: document.title, url: document.URL, links: document.links.length};",
    use_persistent=False,
    api_name="/execute_js"
)
print(result)
```
